# PDF in paper/; auxiliary files in paper/build/ (gitignored).
# This file must stay next to main.tex so that `latexmk -pdf main.tex` finds it.
$pdf_mode = 1;
$aux_dir = 'build';
$out_dir = '.';
$emulate_aux = 1;
