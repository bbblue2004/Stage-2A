# Archived material

This directory contains outputs from superseded exploratory protocols. They
are retained for traceability but are not inputs to the current article or to
the reproducible pipeline in `src/experiments/`.

Current numerical outputs belong in `results/`; figures used by the article
belong in the experiment-specific directories under `figures/`.
